# vibe-coder-workflow
Most AI coding workflows fail two ways: too loose (frankenstein codebase) or too rigid (lost speed). The fix: split the layers. Claude (chat) = thinking. Claude Code = doing. Handoff via one file: next-task.md. Always overwrite, one task at a time. Ship enterprise-grade code at vibe-coder speed.
